language: java

jdk:
    - oraclejdk8

env:
    - TERM=dumb

script:
    - "./gradlew clean check -i --continue"

branches:
  only:
    - master

after_failure:
    - "python bugcomment.py"

addons:
  apt:
    packages:
      - oracle-java8-installer