language: java

jdk:
    - oraclejdk8

env:
    - TERM=dumb

script:
    - "./gradlew clean check -i --continue"

branches:
  only:
    - master

after_failure:
    - "echo 'hi'"

addons:
  apt:
    packages:
      - oracle-java8-installer